package com.comanda.menu;

/** Additional group missing or whose ownership chain (group → product) leads to another tenant. */
public class AdditionalGroupNotFoundException extends RuntimeException {
}
