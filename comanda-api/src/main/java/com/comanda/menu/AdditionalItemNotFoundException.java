package com.comanda.menu;

/** Additional item missing or whose ownership chain (item → group → product) leads to another tenant. */
public class AdditionalItemNotFoundException extends RuntimeException {
}
