/**
 * Owner's menu management: categories, products, additional groups/items and photo upload, all
 * isolated per tenant. Sub-organized by resource ({@code domain}, {@code categories},
 * {@code products}, {@code additionals}, {@code images}), following the {@code auth} slice's
 * pattern from {@code owner-auth}.
 */
package com.comanda.menu;
