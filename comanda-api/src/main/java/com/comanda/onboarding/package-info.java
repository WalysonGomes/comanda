/**
 * Onboarding wizard's thin backend: a demo-cardápio catalog per {@link com.comanda.onboarding.Segment}
 * materialized as real tenant data via {@code menu-management}'s services, and the business-hours
 * write path for step 2 — both orchestration, no cardápio/horário rule reimplemented (PRD Seção
 * 5.1, design.md Decisions 7/8).
 */
package com.comanda.onboarding;
