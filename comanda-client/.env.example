# Comma-separated public hosts. The bare root and its www/app labels render the landing.
VITE_ROOT_DOMAINS=comanda.example
# Preview, local, or alternate hosts that must also render the landing.
VITE_ROOT_HOST_ALIASES=preview.example.dev
